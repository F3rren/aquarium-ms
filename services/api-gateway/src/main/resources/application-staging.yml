# Staging Profile — api-gateway
# Activate with: SPRING_PROFILES_ACTIVE=staging sull'EC2

management:
  endpoint:
    health:
      show-details: always

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty.http.client: DEBUG
